# Инструкция по добавлению недостающих ключей локализации

## ❌ ВАЖНО: Как НЕ надо делать

**НИКОГДА не делайте так** (это затирает весь файл):
```bash
cat zh.json | jq '.accounts.form.newKey = "value"' > zh.json  # ❌ ПЛОХО!
```

## ✅ Правильный способ добавления ключей через jq

### Базовый способ (1 ключ):
```bash
cd web/admin-spa/src/locales

# Шаг 1: Читаем, добавляем, сохраняем во временный файл
cat zh.json | jq '.accounts.form.newKey = "значение"' > zh_tmp.json

# Шаг 2: Проверяем валидность JSON
jq '.' zh_tmp.json > /dev/null && echo "OK" || echo "ERROR"

# Шаг 3: Только после проверки заменяем оригинал
mv zh_tmp.json zh.json
```

### Пакетное добавление (много ключей за раз):
```bash
cd web/admin-spa/src/locales

cat zh.json | jq '
  .accounts.form.key1 = "значение1" |
  .accounts.form.key2 = "значение2" |
  .accounts.form.key3 = "значение3" |
  .accounts.form.key4 = "значение4"
' > zh_tmp.json && \
jq '.' zh_tmp.json > /dev/null && \
mv zh_tmp.json zh.json && \
echo "✓ Добавлено успешно в zh.json"
```

## Пошаговый процесс работы

### 1. Запускаем валидацию и смотрим файлы с проблемами

```bash
npm run locale:validate 2>&1 | grep "📄"
```

**Результат:** Список файлов с недостающими ключами

---

### 2. Для каждого файла делаем:

#### A. Смотрим полный список недостающих ключей (большими кусками!)

```bash
# Показываем 300 строк для файла
npm run locale:validate 2>&1 | grep -A300 "components/accounts/AccountForm.vue" | head -300
```

#### B. Смотрим diff файла с main (большими кусками!)

```bash
# Показываем 500 строк diff
git diff main..HEAD -- web/admin-spa/src/components/accounts/AccountForm.vue | head -500

# Или конкретно для группы ключей
git diff main..HEAD -- web/admin-spa/src/components/accounts/AccountForm.vue | grep -B5 -A2 "keyName1\|keyName2\|keyName3" | head -300
```

#### C. Находим пары ключ-значение (вручную, смотрим на diff)

**Паттерн поиска:**
- Строки с `+` содержат `$t('accounts.form.keyName')` — это новый ключ
- Строки с `-` выше содержат китайский текст — это оригинальное значение
- Игнорируем строки `---` и `+++` (это служебные)

**Пример:**
```diff
-                <label>API 密钥 *</label>
+                <label>{{ $t('accounts.form.apiKeyLabel') }}</label>
```
→ `apiKeyLabel = "API 密钥 *"`

#### D. Добавляем ключи в zh.json (пакетно по 10-20 ключей)

```bash
cd web/admin-spa/src/locales

cat zh.json | jq '
  .accounts.form.key1 = "中文1" |
  .accounts.form.key2 = "中文2" |
  .accounts.form.key3 = "中文3" |
  .accounts.form.key4 = "中文4" |
  .accounts.form.key5 = "中文5" |
  .accounts.form.key6 = "中文6" |
  .accounts.form.key7 = "中文7" |
  .accounts.form.key8 = "中文8" |
  .accounts.form.key9 = "中文9" |
  .accounts.form.key10 = "中文10"
' > zh_tmp.json && \
jq '.' zh_tmp.json > /dev/null && \
mv zh_tmp.json zh.json && \
echo "✓ Добавлено 10 ключей в zh.json"
```

#### E. Добавляем переводы в ru.json и en.json (те же ключи)

```bash
cat ru.json | jq '
  .accounts.form.key1 = "Русский1" |
  .accounts.form.key2 = "Русский2" |
  .accounts.form.key3 = "Русский3" |
  .accounts.form.key4 = "Русский4" |
  .accounts.form.key5 = "Русский5" |
  .accounts.form.key6 = "Русский6" |
  .accounts.form.key7 = "Русский7" |
  .accounts.form.key8 = "Русский8" |
  .accounts.form.key9 = "Русский9" |
  .accounts.form.key10 = "Русский10"
' > ru_tmp.json && \
jq '.' ru_tmp.json > /dev/null && \
mv ru_tmp.json ru.json && \
echo "✓ Добавлено 10 ключей в ru.json"

cat en.json | jq '
  .accounts.form.key1 = "English1" |
  .accounts.form.key2 = "English2" |
  .accounts.form.key3 = "English3" |
  .accounts.form.key4 = "English4" |
  .accounts.form.key5 = "English5" |
  .accounts.form.key6 = "English6" |
  .accounts.form.key7 = "English7" |
  .accounts.form.key8 = "English8" |
  .accounts.form.key9 = "English9" |
  .accounts.form.key10 = "English10"
' > en_tmp.json && \
jq '.' en_tmp.json > /dev/null && \
mv en_tmp.json en.json && \
echo "✓ Добавлено 10 ключей в en.json"
```

---

### 3. Проверяем результат для конкретного файла

```bash
npm run locale:validate 2>&1 | grep -A50 "AccountForm.vue"
```

**Ожидаемый результат:** Количество недостающих ключей должно уменьшиться

---

### 4. Переходим к следующему файлу

Повторяем шаги 2A-2E для следующего файла из списка

---

### 5. Финальная валидация

Когда все файлы обработаны:

```bash
npm run locale:validate
```

**Ожидаемый результат:**
```
✅ Все ключи из Vue компонентов присутствуют во всех файлах локализации!
```

---

## Советы для ускорения работы

1. **Обрабатывайте ключи пакетами по 10-20 штук**
2. **Смотрите diff большими кусками** (500+ строк)
3. **Копируйте паттерны** - многие ключи похожи по структуре
4. **Проверяйте валидность JSON после каждого пакета**
5. **Делайте коммиты после каждого файла** для безопасности

---

## Текущий статус

### Файлы с недостающими ключами:
1. ~~`components/layout/AppHeader.vue`~~ - ✅ ГОТОВО (был CSS класс, исправлен скрипт валидации)
2. `components/accounts/AccountForm.vue` - 93 ключа
3. ~~`components/accounts/OAuthFlow.vue`~~ - ✅ ГОТОВО (добавлено 2 ключа)
4. `views/AccountsView.vue` - 12 ключей

**Итого:** 105 ключей

### Порядок обработки:
1. ~~`OAuthFlow.vue`~~ - ✅ ГОТОВО
2. `AccountsView.vue` - СЛЕДУЮЩИЙ (12 ключей)
3. `AccountForm.vue` - ПОСЛЕДНИЙ (93 ключа, самый большой)
4. Финальная проверка
